# Mirrored from pyproject.toml [project.dependencies] — keep both in sync.
psycopg[binary]==3.2.*
requests==2.32.*
python-dotenv==1.0.*
tenacity==9.0.*

# Observability: the app speaks the Sentry protocol; the DSN points at self-hosted GlitchTip.
sentry-sdk==2.*

# Google Drive + Calendar sinks
google-api-python-client==2.*
google-auth==2.*
google-auth-oauthlib==1.*

# LLM providers (Phase 2). Each adapter imports only its own vendor's official SDK —
# llm/claude.py -> anthropic, llm/openai.py -> openai, llm/gemini.py -> google-genai.
# No adapter is a generic HTTP client pointed at a compatibility shim.
anthropic==0.*
openai==2.*
google-genai==1.*

# dev / test
pytest==8.*
